import { precacheAndRoute } from 'workbox-precaching';

// Precache all assets generated by the build
precacheAndRoute(self.__WB_MANIFEST);

// Ontime.Build push-notification worker
const CACHE_NAME = 'ontime-build-notifications-v1';

self.addEventListener('install', () => {
  self.skipWaiting();
});

self.addEventListener('activate', (event) => {
  event.waitUntil(self.clients.claim());
});

// Handle push notifications
self.addEventListener('push', (event) => {
  if (!event.data) {
    return;
  }

  let data;
  try {
    data = event.data.json();
  } catch (e) {
    data = { title: 'Notification', body: event.data.text() };
  }

  const options = {
    body: data.body,
    icon: '/ontime-logo.png',
    badge: '/ontime-logo.png',
    tag: data.tag || 'general',
    requireInteraction: true,
    actions: data.actions || [
      {
        action: 'view',
        title: 'View',
        icon: '/ontime-logo.png'
      }
    ],
    data: {
      url: data.url || '/',
      ...data.data
    }
  };

  event.waitUntil(
    self.registration.showNotification(data.title || 'Ontime.Build', options)
  );
});

// Handle notification clicks
self.addEventListener('notificationclick', (event) => {
  event.notification.close();

  const url = event.notification.data?.url || '/';

  event.waitUntil(
    self.clients.matchAll({ type: 'window', includeUncontrolled: true })
      .then((clientList) => {
        for (const client of clientList) {
          if (client.url.includes(url.split('?')[0]) && 'focus' in client) {
            return client.focus();
          }
        }

        if (self.clients.openWindow) {
          return self.clients.openWindow(url);
        }
      })
  );
});

// Handle background sync
self.addEventListener('sync', (event) => {
  if (event.tag === 'check-pending-approvals') {
    event.waitUntil(checkPendingApprovals());
  }
});

async function checkPendingApprovals() {
  // Placeholder for background approval polling.
}
